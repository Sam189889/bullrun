{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run db:install && npm run build"
  },
  "deploy": {
    "startCommand": "npm run production",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
