[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm install && npm run db:install && npm run build"]

[start]
cmd = "npm run production"
