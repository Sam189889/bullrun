-- Create database if not exists
CREATE DATABASE IF NOT EXISTS bull_run 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_unicode_ci;

-- Use the database
USE bull_run;

-- Show success message
SELECT 'Database bull_run created successfully!' as message;
